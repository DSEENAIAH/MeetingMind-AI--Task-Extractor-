# Backend Environment Variables
# Copy this file to .env for local development

# Server Configuration
PORT=5000
NODE_ENV=development

# Mock Mode (set to true for demo without API keys)
USE_MOCK=true

# AI Configuration (OpenAI, Claude, or Gemini)
# NOTE: Only needed when USE_MOCK=false
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_AI_API_KEY=...

# Which AI provider to use: openai, anthropic, or google
AI_PROVIDER=openai

# Zoho OAuth Configuration
# Get these from https://api-console.zoho.com/
# ZOHO_CLIENT_ID=1000.XXXXX
# ZOHO_CLIENT_SECRET=xxxxx
# ZOHO_REDIRECT_URI=http://localhost:5000/api/auth/zoho/callback
# ZOHO_SCOPE=ZohoProjects.projects.ALL,ZohoProjects.tasks.ALL

# Default Zoho Project (optional)
# ZOHO_DEFAULT_PORTAL_ID=xxxxx
# ZOHO_DEFAULT_PROJECT_ID=xxxxx

# Session Secret (generate a random string for production)
SESSION_SECRET=your-secret-key-change-in-production

# CORS Settings (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
